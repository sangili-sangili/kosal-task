{
  "name": "enterprise-fullstack-app",
  "version": "1.0.0",
  "description": "Enterprise-grade scalable full-stack web application with React, Vite, Tailwind CSS, Redux, Node.js, Express, Sequelize, MySQL, Redis, and BullMQ.",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"blue,green\" \"npm run dev --workspace=backend\" \"npm run dev --workspace=frontend\"",
    "dev:backend": "npm run dev --workspace=backend",
    "dev:frontend": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=backend && npm run build --workspace=frontend",
    "test": "npm run test --workspace=backend && npm run test --workspace=frontend",
    "lint": "npm run lint --workspace=backend && npm run lint --workspace=frontend",
    "db:migrate": "npm run db:migrate --workspace=backend",
    "db:seed": "npm run db:seed --workspace=backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "keywords": [
    "enterprise",
    "clean-architecture",
    "react",
    "vite",
    "tailwind",
    "redux-toolkit",
    "nodejs",
    "express",
    "sequelize",
    "mysql",
    "redis",
    "bullmq"
  ],
  "author": "Enterprise Architecture Team",
  "license": "ISC"
}
